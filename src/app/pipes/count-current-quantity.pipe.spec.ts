import { CountCurrentQuantityPipe } from './count-current-quantity.pipe';

describe('CountCurrentQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new CountCurrentQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});
